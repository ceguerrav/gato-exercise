import { BlackboardCatDirective } from './blackboard-cat.directive';

describe('BlackboardCatDirective', () => {
  it('should create an instance', () => {
    const directive = new BlackboardCatDirective();
    expect(directive).toBeTruthy();
  });
});
